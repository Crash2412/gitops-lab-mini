apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-local-cert
  namespace: default
spec:
  secretName: demo-local-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - demo.local
